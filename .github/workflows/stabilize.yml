name: Stabilize

on:
  workflow_dispatch:
  schedule:
    - cron: '55 3 * * *'

permissions:
  contents: read

jobs:
  stabilize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: No-op sanity
        run: |
          echo "Workflow $GITHUB_WORKFLOW is syntactically valid and ready."
