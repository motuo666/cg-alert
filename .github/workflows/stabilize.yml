name: Stabilize Ops

on:
  schedule:
    - cron: '0 */12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (if package.json present)
        if: ${{ hashFiles('package.json') != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps (if package.json present)
        if: ${{ hashFiles('package.json') != '' }}
        run: |
          set -euo pipefail
          npm ci || npm i
- name: Stabilization tasks
  run: |
   set -euo pipefail

   if [ -f "scripts/stabilize.sh" ]; then
     echo "→ Running: scripts/stabilize.sh"
     case ".sh" in
       .mjs|.js)
         node "scripts/stabilize.sh"
         ;;
       .sh)
         bash "scripts/stabilize.sh"
         ;;
       *)
         echo "Unknown extension for scripts/stabilize.sh, trying bash"
         bash "scripts/stabilize.sh"
         ;;
     esac
     exit 0
   fi

   if [ -f "scripts/stabilize.mjs" ]; then
     echo "→ Running: scripts/stabilize.mjs"
     case ".mjs" in
       .mjs|.js)
         node "scripts/stabilize.mjs"
         ;;
       .sh)
         bash "scripts/stabilize.mjs"
         ;;
       *)
         echo "Unknown extension for scripts/stabilize.mjs, trying bash"
         bash "scripts/stabilize.mjs"
         ;;
     esac
     exit 0
   fi

   if [ -f "scripts/stabilize.js" ]; then
     echo "→ Running: scripts/stabilize.js"
     case ".js" in
       .mjs|.js)
         node "scripts/stabilize.js"
         ;;
       .sh)
         bash "scripts/stabilize.js"
         ;;
       *)
         echo "Unknown extension for scripts/stabilize.js, trying bash"
         bash "scripts/stabilize.js"
         ;;
     esac
     exit 0
   fi
   echo "skip: none of expected scripts found"; exit 0
