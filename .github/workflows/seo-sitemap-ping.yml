name: SEO Sitemap & IndexNow

on:
  schedule:
    - cron: '20 3 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: SEO sitemap ping retired
        run: echo "SEO sitemap and IndexNow ping are handled by CG Full Pipeline; this workflow is now a no-op."

concurrency:
  group: cg-full-pipeline
  cancel-in-progress: false
