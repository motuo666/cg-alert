name: Build Content

on:
  schedule:
    - cron: '15 */6 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Build content retired
        run: echo "Build Content is handled by CG Full Pipeline; this workflow is now a no-op."

concurrency:
  group: cg-full-pipeline
  cancel-in-progress: false
