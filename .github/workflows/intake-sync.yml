name: Intake Sync

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Intake sync retired
        run: echo "Intake sync is handled by external services; this workflow is now a no-op."

concurrency:
  group: cg-full-pipeline
  cancel-in-progress: false
