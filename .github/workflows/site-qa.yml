name: Site QA (links+policy+assets)
on:
  workflow_dispatch: {}
permissions:
  contents: write
jobs:
  qa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Link Check
        run: node scripts/qa/link_check.js
      - name: Policy Check (pricing only on home, canonical www)
        run: node scripts/qa/policy_check.js
      - name: Assets Check
        run: node scripts/qa/assets_check.js
      - name: Commit artifacts
        run: |
          set -e
          git config user.email "bot@cg-alert.com"
          git config user.name  "cg-alert-bot"
          git add artifacts/*.json || true
          if git diff --cached --quiet; then echo 'no changes'; else git commit -m "qa: link+policy+assets checks"; git pull --rebase --autostash || true; git push; fi
