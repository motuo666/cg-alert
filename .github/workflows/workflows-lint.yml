name: Workflows Lint
on:
  pull_request:
  push:
    paths:
      - ".github/workflows/**.yml"

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: rhysd/actionlint@v1
