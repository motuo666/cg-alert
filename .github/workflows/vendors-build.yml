name: Vendors Build

on:
  workflow_dispatch:
    inputs:
      dry:
        description: "Dry run? (true=只评估, false=真实提交)"
        required: true
        default: "true"
        type: choice
        options: ["true","false"]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # 你之前说“public不用，直接根目录”，这里默认 .
      PUBLISH_DIR: ${{ vars.PUBLISH_DIR || '.' }}
      NODE_OPTIONS: --max-old-space-size=2048
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install deps
        run: npm ci || npm install

      - name: Build vendors index
        run: node scripts/build_vendors_index.js

      # 下面三步负责把 evidence 渲染成 /reports 与 RSS
      - name: Render evidence pages
        run: node scripts/render_evidence_pages.js

      - name: Generate /reports index
        run: node scripts/generate_reports_index.js

      - name: Build RSS (/rss/index.xml)
        run: node scripts/build_reports_rss.js

      - name: Commit & push content artifacts
        run: |
          set -e
          git config user.email "bot@cg-alert.com"
          git config user.name  "cg-alert-bot"
          # 只提交内容产物，不碰 UI 页面
          git add "$PUBLISH_DIR/vendors" \
                  "$PUBLISH_DIR/api" \
                  "$PUBLISH_DIR/reports" \
                  "$PUBLISH_DIR/rss/index.xml" \
                  "$PUBLISH_DIR/sitemap.xml" || true
          if git diff --cached --quiet; then
            echo "no changes to commit"
          else
            git commit -m "Vendors build: vendors/api/reports/rss artifacts"
            git pull --rebase --autostash || true
            git push
          fi
