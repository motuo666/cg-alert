name: On Stripe Paid → Customer Scaffold

on:
  repository_dispatch:
    types: [stripe_paid]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Customer scaffold retired
        run: echo "Customer scaffold is handled outside this repo; this workflow is now a no-op."

concurrency:
  group: cg-full-pipeline
  cancel-in-progress: false
