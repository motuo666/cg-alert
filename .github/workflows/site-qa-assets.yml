name: Site QA (Assets & Links)

on:
  schedule:
    - cron: '30 3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (if package.json present)
        if: ${{ hashFiles('package.json') != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install deps (if package.json present)
        if: ${{ hashFiles('package.json') != '' }}
        run: |
          set -euo pipefail
          npm ci || npm i
- name: Check links/assets
  run: |
   set -euo pipefail

   if [ -f "scripts/linkcheck.mjs" ]; then
     echo "→ Running: scripts/linkcheck.mjs"
     case ".mjs" in
       .mjs|.js)
         node "scripts/linkcheck.mjs"
         ;;
       .sh)
         bash "scripts/linkcheck.mjs"
         ;;
       *)
         echo "Unknown extension for scripts/linkcheck.mjs, trying bash"
         bash "scripts/linkcheck.mjs"
         ;;
     esac
     exit 0
   fi

   if [ -f "scripts/linkcheck.js" ]; then
     echo "→ Running: scripts/linkcheck.js"
     case ".js" in
       .mjs|.js)
         node "scripts/linkcheck.js"
         ;;
       .sh)
         bash "scripts/linkcheck.js"
         ;;
       *)
         echo "Unknown extension for scripts/linkcheck.js, trying bash"
         bash "scripts/linkcheck.js"
         ;;
     esac
     exit 0
   fi
   echo "skip: none of expected scripts found"; exit 0
