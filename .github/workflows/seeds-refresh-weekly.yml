name: Seeds Refresh Weekly

on:
  schedule:
    - cron: '30 4 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Seeds refresh retired
        run: echo "Seeds refresh is handled outside this repo; this workflow is now a no-op."

concurrency:
  group: cg-full-pipeline
  cancel-in-progress: false
