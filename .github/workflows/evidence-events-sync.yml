name: Evidence Events Sync

on:
  schedule:
    # 每天凌晨 03:20 UTC 跑一次；也可以手动 workflow_dispatch
    - cron: "20 3 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Promote high-signal evidence into data/events.*
        run: |
          set -euo pipefail
          python3 scripts/promote_evidence_events.py --root .

      - name: Build canonical events.csv from raw/backlog
        run: |
          set -euo pipefail
          python3 scripts/build_events_canonical.py --root .
          python3 scripts/normalize_events.py --root .
          python3 scripts/check_data_schema.py --root .

      - name: Commit and push if changed
        run: |
          set -euo pipefail
          if git diff --quiet; then
            echo "No changes; nothing to commit."
            exit 0
          fi
          git config user.name "cg-alert-bot"
          git config user.email "bot@cg-alert.com"
          git add data/events.raw.csv data/events.backlog.csv data/events.csv
          git commit -m "chore(ci): promote evidence and rebuild canonical events feed"
          git push

concurrency:
  group: cg-full-pipeline
  cancel-in-progress: false
