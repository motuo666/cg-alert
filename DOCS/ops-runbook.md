# Ops Runbook (Autopilot)
- GitHub Actions schedules run harvest → enrich → outreach → intake.
- Stripe reconcile runs hourly to capture new customers.
- Bounces and unsubscribes sync nightly to suppression list.
- Alerts are posted to Slack on failures.
