name: Autopilot Step
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  autopilot:
    runs-on: ubuntu-latest
    steps:
      - name: Autopilot retired
        run: echo "Autopilot has been retired; this workflow is a no-op."
